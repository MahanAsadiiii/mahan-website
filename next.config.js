/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  publicRuntimeConfig: {
    staticFolder: '/public/fonts',
  },
};

module.exports = nextConfig
